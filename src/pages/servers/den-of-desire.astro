---
import Layout from '../../layouts/Layout.astro';
import ServerTabs from '../../components/servers/ServerTabs.astro';
import { getServer } from '../../data/servers.js';

const server = getServer('den-of-desire')!;
---
<Layout title={server.name}>
  <ServerTabs server={server} />
</Layout>
